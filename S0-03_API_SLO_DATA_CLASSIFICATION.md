# S0-03：OpenAPI 约定、错误码、SLO 与数据分级

状态：待负责人冻结
依赖：S0-01、S0-02、PLAN_V6 0.4/0.5
范围：冻结接口、失败语义、服务目标和数据处理边界；不实现业务代码、数据库迁移、支付或退款集成

## 1. OpenAPI 约定

### 1.1 通用约定

| 项目 | 约定 |
|---|---|
| 根路径 | `/api/v1`；版本只在路径中体现，资源字段不重复版本号 |
| 媒体类型 | 请求/响应默认 `application/json; charset=utf-8`；文件证据使用受控上传接口和对象引用，不把二进制塞进 JSON |
| 时间 | RFC 3339 UTC，响应统一带时区；服务端生成 `created_at/updated_at` |
| ID | 使用不暴露数据库顺序的资源 ID；客户端不得根据 ID 推断资源存在性或归属 |
| 认证 | 浏览器使用短生命周期、`HttpOnly`、`Secure`、`SameSite` Cookie；状态变更请求必须有 CSRF 防护；不把长期高权限 token 放入 `localStorage` |
| 请求关联 | 每个请求接受或生成 `X-Request-ID`；响应返回 request ID，并通过 trace ID 关联日志、指标和审计 |
| 分页 | 列表默认分页；使用 cursor 或明确约定的 page/limit；limit、过滤字段、排序字段必须白名单校验 |
| 空结果 | 合法列表查询返回 200 + 空数组及分页信息，不用 404 表示列表为空 |
| 命令接口 | 改变状态的客户/内部角色命令使用 `POST`；这些命令必须要求 `Idempotency-Key`；审批命令额外要求 `expected_version`；外部支付 callback 不适用该请求头 |
| AI 流式 | SSE 只用于低风险 AI 建议/解释展示；退款申请、确认、审批、回调等可靠命令使用普通 HTTP 请求 |
| 金额 | 禁止浮点数；最小货币单位、货币码、支付事实来源和全额公式由 D06 冻结后写入具体 schema |
| OpenAPI | OpenAPI 文档是前后端唯一契约；接口变更必须同步请求、响应、错误码、权限范围和示例 |

响应不得返回 `password_hash`、token、密钥、支付凭证或超出当前角色/资源范围的 PII。错误信息必须可操作但不能泄露他人资源是否存在。

### 1.2 第一条闭环命令边界

以下是 PLAN_V6 0.5 的契约目录；本文件不实现这些接口。

| 方法与路径 | 调用者 | 成功语义 | 关键拒绝条件 |
|---|---|---|---|
| `POST /api/v1/after-sales` | `customer` | 创建 `SUBMITTED` 售后申请 | 非本人、非 `PAID`、`UNMATCHED`、重复有效申请、事实不完整 |
| `POST /api/v1/after-sales/{id}/cancel` | `customer` | 在合法状态取消申请 | 非本人、状态不允许取消、已进入不可逆处理；精确状态由 D04 冻结 |
| `POST /api/v1/after-sales/{id}/claim` | `agent` | 原子认领并获得白名单详情范围 | 已被认领、不在客服队列、角色或范围不足 |
| `POST /api/v1/after-sales/{id}/release` | 当前 `agent` | 在不可逆动作前释放认领 | 非当前负责人、状态不允许释放；S0-02 已定义超时回收方向 |
| `POST /api/v1/after-sales/{id}/request-evidence` | 当前 `agent` | 转入 `EVIDENCE_PENDING` 并通知客户 | 未认领、状态不匹配、证据规则未满足 |
| `POST /api/v1/after-sales/{id}/review` | 当前 `agent` | 提交人工意见，直接拒绝或进入财务审批 | 未认领、版本冲突、状态不匹配、政策依据缺失 |
| `POST /api/v1/after-sales/{id}/confirm-auto-refund` | `customer` | 确认有效报价并产生退款 Outbox | 非本人、非 `PENDING_CUSTOMER_CONFIRMATION`、报价过期、版本冲突 |
| `POST /api/v1/after-sales/{id}/approve` | `finance` | 批准并产生退款 Outbox | 非 `PENDING_FINANCE_APPROVAL`、版本冲突、审批分离规则不满足 |
| `POST /api/v1/after-sales/{id}/reject` | `finance` | 驳回申请并记录原因 | 非 `PENDING_FINANCE_APPROVAL`、版本冲突、范围不足 |
| `POST /api/v1/payments/alipay/callback` | 支付网关 | 接收并幂等处理回调 | 不要求 `Idempotency-Key`；签名、商户、稳定外部事件标识、商户退款请求号、外部退款流水、订单、金额、货币、状态或时间窗口校验失败 |
| `GET /api/v1/audit-events` | `admin` 或有资源范围的内部角色 | 读取允许范围内的审计元数据 | 无对应资源范围；默认不返回完整 PII |

退款执行没有浏览器或 Agent 公开命令接口。`refund-worker` 只消费已授权 Outbox；支付回调只能进入 callback endpoint，由应用服务验签、幂等处理并推进领域状态。

### 1.3 幂等与并发

- 同一个 `Idempotency-Key` 加同一调用者、同一路径和同一请求摘要，应返回第一次处理的同一业务结果；不能重复创建申请、Outbox 或退款。
- 同一个 key 对应不同请求摘要，返回 `IDEMPOTENCY_CONFLICT`，不得覆盖原请求。
- 幂等记录保留期限和清理策略需在发布前冻结；未冻结前不得实现自动清理。
- 支付 callback 不使用 `Idempotency-Key`；应用服务以支付网关稳定外部事件标识、商户退款请求号和外部退款流水的唯一组合进行去重，具体字段在 S5 PaymentGateway 契约中冻结。
- `expected_version` 不匹配返回 `VERSION_CONFLICT`；客户端必须重新读取后再决定是否重试。
- 网络超时不能推断命令失败；客户端应使用原 key 查询/重试，不能生成新 key 规避幂等。

## 2. 统一错误响应与错误码

### 2.1 响应结构

```json
{
  "error": {
    "code": "VERSION_CONFLICT",
    "message": "资源已发生变化，请刷新后重试",
    "details": {},
    "request_id": "req_opaque_id"
  }
}
```

约定：`code` 稳定、可编程、使用大写下划线；`message` 面向当前调用者且可本地化；`details` 只放非敏感的字段级修复提示；`request_id` 用于人工排障。内部日志和审计可以保存更详细的拒绝原因，但不能原样返回给无权限调用者。

### 2.2 错误码目录

| HTTP | 错误码 | 使用场景 | 客户端处理 |
|---:|---|---|---|
| 400 | `INVALID_REQUEST` | JSON、字段格式或命令结构错误 | 修正请求后重试，不重用错误 payload 的 key |
| 401 | `AUTHENTICATION_REQUIRED` / `TOKEN_INVALID` | 未登录、登录态失效或 Redis 校验失败 | 重新登录 |
| 403 | `FORBIDDEN` | 已认证但没有该角色或命令权限 | 不重试，提示联系管理员/负责人 |
| 404 | `RESOURCE_NOT_AVAILABLE` | 资源不存在、非本人、未认领详情或 `UNMATCHED` | 统一提示，不泄露资源存在性 |
| 409 | `VERSION_CONFLICT` | 乐观锁版本不一致 | 重新读取后由用户决定 |
| 409 | `IDEMPOTENCY_CONFLICT` | 同 key 对应不同请求摘要 | 使用原 key 查询，禁止换 key 重复资金动作 |
| 409 | `DUPLICATE_ACTIVE_REQUEST` | 同一订单已有有效售后申请 | 查看已有申请，不创建第二条 |
| 409 | `INVALID_STATE` | 当前状态不允许该命令 | 刷新状态并按下一步处理 |
| 422 | `ORDER_NOT_ELIGIBLE` | 已确认订单但不满足退款事实/政策 | 展示安全原因和人工求助入口 |
| 422 | `EVIDENCE_REQUIRED` | 必需证据缺失或不合规 | 按提示补证 |
| 422 | `QUOTE_EXPIRED` | 自动退款报价或确认期限已过 | 重新进入评估/人工路径 |
| 400 | `PAYMENT_CALLBACK_INVALID` | 回调签名、商户、流水、金额、货币或状态校验失败 | 支付方重试或进入异常队列，不能推进退款 |
| 503 | `POLICY_UNAVAILABLE` | 策略服务不可用 | 停止自动放行，转人工/财务 |
| 503 | `DEPENDENCY_UNAVAILABLE` | 数据库、支付、通知或其他关键依赖暂不可用 | 按接口降级说明重试或转人工 |
| 429 | `RATE_LIMITED` | 超过接口/角色/资源限流 | 按 `Retry-After` 等待 |
| 500 | `INTERNAL_ERROR` | 未预期服务错误 | 使用 request ID 联系运维，不盲目重试资金命令 |

资源范围拒绝优先使用 `RESOURCE_NOT_AVAILABLE`，不把“他人的订单存在但不可访问”返回成可枚举的 403。面向客户的订单/售后入口，对订单不存在、非本人订单和 `UNMATCHED` 统一返回 `404 RESOURCE_NOT_AVAILABLE`。客户界面只能展示通用提示“订单不可用或无法核验，并可申请人工核验”，不得指出订单是否存在或具体处于 `UNMATCHED` 状态。

`UNMATCHED_ORDER` 仅作为内部审计原因码，或用于已经进入独立人工核验工单后的受控内部流程，不得作为客户接口错误码。支付 callback 的具体 HTTP 响应、网关重试语义和错误码映射需与支付适配器契约一并冻结。

## 3. SLO 与可观测性

### 3.1 PLAN_V6 初版目标

以下沿用 PLAN_V6 的初始目标，不新增更高承诺：

| 能力 | 初版 SLO/目标 | 降级行为 | 必须关注 |
|---|---|---|---|
| 订单/售后查询 | 月可用性 99.9%；不含 AI 的 P95 < 500ms | AI 不可用时仍返回确定性订单事实并引导人工 | 5xx、P95、DB 连接池、资源越权拒绝 |
| 退款状态 | 100% 不重复、不越权、不丢审计；批准命令 P95 < 1s | 暂停自动发起，保留待办并人工处理 | 非法状态、重复事件、死信、对账差异 |
| 退款最终收敛 | 外部支付状态在 15 分钟内收敛，否则进入人工异常队列 | 保持处理中/异常，不重复发起 | 长时间处理中、回调失败、重试耗尽 |
| LLM/RAG | 非关键依赖；首 token P95 < 5s 为体验目标 | 停用 AI 建议，核心工作台可人工完成 | 超时、熔断、评测下滑、成本突增 |
| Redis | 认证 fail-closed；不保存资金真相 | 明确限制登录/会话能力，不随机 500 | 不可用率、内存和连接使用 |

月度窗口、计划维护、错误预算、SLO 统计分母、告警通知人和最终阈值由负责人/运维在发布前确认；本文件不把体验目标误当作资金正确性保证。

### 3.2 指标和关联字段

所有指标不包含密码、token、支付凭证或原始 PII，至少带以下维度：`service`、`route`、`method`、`status_code`、`error_code`、`role`、`dependency`、`environment`。请求日志、trace、指标和审计使用 `request_id/trace_id` 关联。

建议指标：

- HTTP 请求量、4xx/5xx、P50/P95/P99、超时和限流数；
- DB 连接池使用率、Redis 校验失败/不可用、LLM 延迟和 token 成本；
- 售后各状态停留时长、客服/财务待办积压、Outbox 重试/死信；
- 退款重复尝试、回调验签失败、状态不匹配和对账异常；
- AI 建议生成、人工采用/修改/拒绝，但不记录未脱敏的 prompt 或证据原文。

## 4. 数据分级与处理规则

### 4.1 分级

| 级别 | 含义 | 示例 | 基本处理 |
|---|---|---|---|
| L0 Public | 可公开或无需身份即可展示的非敏感内容 | 已批准公开的商品知识、一般政策说明 | 可缓存；不得混入内部订单事实 |
| L1 Internal | 仅项目内部使用，不含直接个人或资金秘密 | 脱敏聚合报表、系统健康、非敏感配置元数据 | 需要内部身份；日志可记录摘要 |
| L2 Confidential | 受资源范围保护的业务或个人数据 | 用户名、姓名、手机号、地址、订单/订单项、工单、聊天、证据、退款申请、政策输入事实 | 最小权限；按 owner/assigned scope；日志和 AI 输入脱敏/白名单 |
| L3 Restricted | 泄露会导致账号、资金或合规事故的数据 | `password_hash`、access token、API key、支付凭证/签名、密钥、原始敏感证据、完整外部支付报文 | 仅专用服务/最小主体；禁止返回、进入日志、进入普通 AI prompt 或普通报表 |

### 4.2 角色访问边界

| 数据 | customer | agent | finance | operator | admin | refund-worker |
|---|---|---|---|---|---|---|
| 自己订单/售后/聊天 | 自己完整范围 | 不可默认读取 | 仅关联审批范围 | 不可读原始记录 | 不默认读取 | 仅关联退款事实 |
| 未认领工单 | 不适用 | 脱敏摘要 | 不适用 | 聚合指标 | 审计元数据 | 不适用 |
| 已认领工单 | 不适用 | 审核白名单字段 | 关联审批所需 | 不可读原始 PII | 不默认读取 | 不适用 |
| 支付/退款事实 | 自己申请的必要脱敏结果 | 审核必要最小字段 | 审批和异常所需 | 不读流水明细 | 不默认读取 | 授权事件所需 |
| 政策内容/版本 | 面向客户的有效说明 | 审核所需有效版本 | 审批依据 | 创建/测试/发布范围 | 仅系统配置，不含政策内容 | 执行时的 policy_version |
| 审计 | 自己操作结果 | 自己相关资源 | 自己审批/异常 | 自己政策/聚合 | 脱敏元数据 | 自己事件轨迹 |

访问矩阵不能被前端、Prompt、MCP 参数或客户端自报角色绕过；服务端每次读取和命令都重新计算 scope。

### 4.3 存储、日志、AI 和留存

- API 响应默认最小字段；日志不得写密码、token、密钥、支付签名、完整证据和未脱敏 PII。
- AI 只接收当前角色 scope 内、按字段白名单筛选后的事实；保存模型/提示词/检索/政策版本、引用 ID、未知项和人工最终意见，不保存无必要的秘密或原始敏感证据。
- 支付原始报文和签名只进入受控 callback/支付审计边界；普通客服、运营和 AI 不可见。
- 账号删除先停用并撤销登录态，不做 `users` 级联硬删除；活跃售后、退款争议、审计和合法留存例外必须继续保留或按批准流程匿名化。
- 聊天、证据、审计和支付数据的具体保留期限、匿名化字段、例外审批人和删除任务触发方式待负责人冻结；冻结前不得实现自动清理。

## 5. 文档冻结门槛

S0-03 冻结前必须由负责人确认：

1. D01–D12 在 S0-01/S0-02 中登记的位置与影响；
2. 金额、币种、幂等记录、callback 响应和支付适配器的具体契约；
3. SLO 统计窗口、错误预算、告警阈值和责任人；
4. L2/L3 字段清单、AI 禁入字段、证据/聊天/审计留存与删除规则。

本文件只定义契约和边界，不代表接口、错误码、SLO 或数据清理已经在代码中实现。
