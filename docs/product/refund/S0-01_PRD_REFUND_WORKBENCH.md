# S0-01：AI 售后与退款运营工作台 PRD

状态：负责人已接受；D01–D12 已冻结
范围：PLAN_V6 第一条闭环，仅覆盖“已支付订单的全额退款”

冻结依据：`S0_DECISION_RECORD.md`

## 1. 产品目标与边界

为客户、客服和财务提供一条可追溯的售后退款工作流：客户提交申请，系统验证订单事实和归属，AI 汇总订单/政策/证据并给出可解释建议，确定性规则决定自动资格或转人工，最终由客户确认或财务审批后进入受控退款流程。

第一版的成功不是“让 AI 自动打款”，而是让 AI 提高审核效率，同时保证资金动作不越权、不重复、可审计、可人工接管。

首版只支持 CNY、已支付、已确认归属、未发货且支付成功后 7 个自然日内的订单。已发货或已签收订单不进入首版退款闭环，转人工售后。`customer_user_id` 为空或无法确认归属的历史订单为 `UNMATCHED`：不向客户暴露，不进入退款流程，不得根据 UID 后缀、手机号或姓名自动回填。

## 2. 角色目标与边界

| 角色 | 目标与允许动作 | 明确禁止 |
|---|---|---|
| `customer` 客户 | 查看自己的订单；为自己的已支付、已确认归属、未发货且在 7 个自然日内的订单创建/取消售后申请；补充证据；确认合格的自动退款报价；查看自己的处理状态 | 查看他人数据；审批或执行退款；访问内部工作台 |
| `agent` 客服 | 查看脱敏未认领队列并原子认领；查看自己认领的完整工单、客户和订单；请求补证；按政策审核并形成建议 | 批准资金退款；调用支付网关；查看其他客服已认领的完整数据 |
| `finance` 财务 | 查看 `PENDING_FINANCE_APPROVAL` 及关联记录；批准或驳回退款；处理异常待办 | 修改订单金额；直接修改退款结果；管理用户或权限 |
| `operator` 运营 | 维护版本化退款政策/商品规则；查看脱敏聚合运营数据 | 查看完整 PII；审批或执行退款；直接修改交易事实 |
| `admin` 管理员 | 管理用户、角色授权、策略系统配置（版本框架/权限/开关，不含具体退款政策内容）和脱敏审计元数据 | 创建、审核、发布或回滚具体退款政策；默认查看订单 PII；处理售后；审批或执行退款；紧急越权 |
| `refund-worker` 机器主体 | 仅消费“财务已批准”或“策略合格且客户已确认”的退款 Outbox 事件，调用支付适配器并写入结果 | 创建申请；绕过规则、审批或确认；修改用户、订单或审批数据 |

所有访问同时受角色权限和资源范围限制。第一版是单商家部署，不引入空壳 `tenant_id`；不实现客服团队、会签或复杂转派系统。

客户不能退款时，系统必须返回可理解的原因，但不能通过错误差异泄露他人订单。对未确认归属、订单不存在或当前用户无权访问的情况统一使用“订单不可用或无法核验”；对已确认属于当前客户的订单，才允许返回更具体的原因：未支付、已退款、退款处理中、超过申请期限、已有有效申请、订单事实不满足政策。每个拒绝结果都要记录内部原因码和审计事件。

`UNMATCHED` 订单不直接进入售后。客户可以从“找不到订单/需要人工核验”入口提交订单号、支付凭证等材料，系统创建独立的人工核验工单；客服按最小字段范围核验，成功绑定必须经过带依据、操作者、时间和审计事件的人工映射流程，不能由 AI、手机号或编号猜测完成。核验成功后是否允许原申请继续，需按负责人确认的人工映射规则执行。

## 3. 第一条业务路径

```text
共同起点：客户登录
  → 选择本人已确认归属且状态为 PAID 的订单
  → 提交全额退款申请
  → 系统校验归属、已支付、未退款、重复申请和必要事实
  → 受控资格评估分流

低风险路径：
  → 应用服务/策略引擎评估自动资格（事实完整、未发货、无重复有效申请、无历史异常、全额退款不超过 2000 CNY）
  → 展示系统计算的全额退款报价和依据
  → 客户确认
  → 应用服务写入退款 Outbox

人工路径：
  → 不满足自动资格条件；已发货/已签收订单转独立人工售后
  → 客服原子认领、补证和审核
  → 进入财务审批，由单个 finance 用户批准/驳回，或在适用的人工售后入口处理
  → 财务批准后应用服务写入退款 Outbox

共同结束：
  → refund-worker 调用支付适配器，处理回调和对账
  → 更新退款、售后和订单状态，记录审计并通知客户
```

低风险路径不要求所有申请先经过客服；只有同时满足 D02 的条件才可进入客户确认路径，其他退款申请一律转财务审批。客服可以在财务路径前认领、补证和形成审核意见，但不能替代财务审批。自动资格仍由确定性策略引擎判定，AI 不得替代该判定。

状态必须由应用服务执行，不能由路由、前端、工具或 LLM 直接写状态字段：

- `Order`：支付状态、履约状态、售后状态分开保存。`AFTER_SALE_OPEN` 是由有效售后申请推导出的业务视图，不覆盖支付或履约事实；退款成功后才记录相应退款事实，不能把售后视图当作支付状态迁移。
- `AfterSaleRequest`：`SUBMITTED → EVIDENCE_PENDING / UNDER_REVIEW / CANCELLED`；`EVIDENCE_PENDING → EVIDENCE_PENDING / UNDER_REVIEW / CANCELLED`；`UNDER_REVIEW → PENDING_CUSTOMER_CONFIRMATION / PENDING_FINANCE_APPROVAL / CANCELLED`；`PENDING_CUSTOMER_CONFIRMATION → REFUND_PROCESSING / CANCELLED / EXPIRED`；`PENDING_FINANCE_APPROVAL → REFUND_PROCESSING / REJECTED`；`REFUND_PROCESSING → REFUNDED / REFUND_EXCEPTION`。
- `Refund`：`CREATED → PROCESSING → SUCCEEDED / FAILED / RECONCILIATION_EXCEPTION`；回调只能推进与外部流水匹配的退款。

所有客户/内部角色命令必须具备资源范围校验和幂等键；审批命令还必须携带 `expected_version`，并在状态变更时写审计事件。支付 callback 不使用 `Idempotency-Key`，按外部事件组合去重。

根据 D03，`AFTER_SALE_OPEN` 只由有效售后申请推导，支付状态、履约状态和售后申请状态分别保存。申请取消、拒绝或过期时，不得覆盖支付或履约事实，也不得把订单状态错误回退为 `PAID`。

第一版客户不填写退款金额，系统根据支付流水以“分”的整数计算 CNY 全额退款报价。可退余额 = 原支付金额 - 已成功退款金额 - 已创建/处理中的退款保留金额。客户只选择原因、提交证据并确认报价。客户仅可在 `SUBMITTED`、`EVIDENCE_PENDING`、`UNDER_REVIEW`、`PENDING_CUSTOMER_CONFIRMATION` 取消；补证最多 2 次、每次 72 小时。`CANCELLED` 可在原 7 个自然日内重新申请一次；`REJECTED` 或 `EXPIRED` 后只能走人工核验/客服入口。

客户每次状态变化都应看到：当前状态、下一步处理角色、是否需要补证、补证截止时间、预计处理时限、退款将退回的已脱敏原支付渠道。通知失败不能改变业务状态，必须进入可重试通知任务或人工待办。

客服命令边界：`claim` 只允许认领未认领且仍在客服队列的申请；`release` 只允许当前负责人在不可逆动作前释放；`request_evidence` 只允许已认领的 `UNDER_REVIEW` 申请并进入 `EVIDENCE_PENDING`；`submit_review` 只提交客服意见和证据引用，不直接批准退款或写入支付状态。证据仅允许 JPEG、PNG、WebP、PDF；单文件最大 10 MB、每次申请最多 5 个文件，申请关闭后保留 180 天。原始证据不得进入日志或 AI Prompt。

## 4. AI 能力与禁止事项

AI 可以：

- 识别客户退款意图并提取申请事实；
- 检索订单、退款政策和客户提交的证据；
- 总结时间线、缺失材料、风险信号和政策依据；
- 生成客服审核草稿、面向客户的解释和人工处理建议；
- 触发“评估自动退款资格”的请求。

AI 不可以：

- 猜测订单归属、用户身份、退款金额或政策例外；
- 自行决定是否合格、直接批准/驳回、改变订单或退款状态；
- 直接调用支付网关、创建 Outbox 或重复发起退款；
- 绕过客户二次确认、财务审批、幂等校验或资源授权；
- 向客户或内部角色泄露其数据范围之外的 PII；
- 通过 Prompt、工具参数或自然语言诱导扩大权限。

自动退款资格由版本化确定性策略引擎决定。AI 只能提供事实整理和评估触发，不能传入“应退金额”或“应当放行”的结论。

财务审批至少展示：订单号、原支付金额、已退款金额、本次全额退款金额、货币、支付渠道、外部支付流水号、退款原因、客服最终意见、政策版本、风险命中原因、客户确认记录和历史退款记录；字段脱敏和可见范围在 S0-03 数据分级中冻结。金额使用“分”的整数，仅支持 CNY，唯一来源为支付流水；可退余额 = 原支付金额 - 已成功退款金额 - 已创建/处理中的退款保留金额。

同一人不得既提交客服审核意见又审批同一申请；首版不做双人审批，超过自动资格范围的申请由单个 `finance` 用户审批。支付回调不得只凭订单号更新状态，至少验证签名、商户/应用标识、外部退款流水号、订单号、金额、货币和当前退款状态。支付失败可以由授权财务人员发起受幂等约束的重试；不能创建绕过原申请的新退款，超过重试策略或无法对账时进入 `RECONCILIATION_EXCEPTION`。

运营政策必须具备生命周期：`DRAFT → TESTING → PENDING_RELEASE → ACTIVE → RETIRED`；记录创建人、审核人、发布人、生效/失效时间、适用商品/订单范围和回滚版本。创建/测试与审核/发布必须由两个不同的 `operator` 身份完成；无法满足职责分离时保持当前 `ACTIVE` 版本，不发布新政策。`admin` 只管理策略系统配置与授权。已用于历史审核的政策不可原地编辑；运营只能修改退款规则，不得修改价格、支付事实、已退款金额或订单状态。

## 5. 成功指标与失败处理

首版必须满足的安全指标：

1. 未发生跨用户订单/工单/会话读取；
2. 未发生重复退款；
3. 每次状态变更均可由审计记录重建；
4. 任何退款执行都能追溯到客户确认或财务批准；
5. AI 不可用时，人工审核和退款流程仍可继续，且不会默认放行。

已冻结的效率指标：自动资格率 = 合格评估数 / 已完成资格评估数；处理时长 = `SUBMITTED` 至最终状态；AI 建议采用、修改或拒绝必须由客服显式选择，不能靠文本猜测。首批 100 个已关闭申请或运行 30 天后再评估效率指标，不预设采用率或时长改善数值。

运营只看脱敏聚合指标：申请量、通过/拒绝率、客服/财务处理时长、自动资格率、补证率、退款异常率、重复申请率，以及按商品、渠道、原因的趋势；所有指标必须固定统计时区、时间窗口、去重口径和状态截止时间，不能用任意 SQL 直接修改交易事实。

失败处理：

- 订单归属/权限失败：返回不泄露资源存在性的拒绝结果；不进入退款流程。
- 不满足 D02 自动资格条件：一律进入 `PENDING_FINANCE_APPROVAL`；已发货/已签收订单转人工售后，不进入首版退款闭环。策略服务不可用时禁止默认放行。
- 客户未确认：停留在 `PENDING_CUSTOMER_CONFIRMATION`，不创建退款 Outbox。
- 并发版本冲突：拒绝本次命令并要求重新读取，不覆盖他人处理结果。
- 支付超时、重复/乱序回调或对账差异：保持可追踪的处理中/异常状态，由 worker 重试或转人工，不重复发起资金动作。
- AI、RAG 或外部服务不可用：展示确定性订单事实和明确降级提示，转客服/财务人工处理。

人工接管入口固定为：客服补证/审核队列、财务审批队列、退款异常/对账差异队列和已发货/已签收订单的人工售后入口。支付失败、回调异常和对账差异由 `finance` 最终关闭；`operator` 只处理政策问题；`admin` 只处理系统配置和账号权限，不作为业务 break-glass 操作者。

客服异常处理：当前负责人可以在不可逆业务动作前释放自己的认领；认领超过 24 小时且仍未进入 `PENDING_FINANCE_APPROVAL` 或 `REFUND_PROCESSING` 时自动回收，并记录原因和审计事件。客服离职、主管转派和跨团队转派首版人工线下处理，不实现复杂团队模型。

客服完整详情采用字段白名单，不等同于“客户全部数据”：只提供审核所需的订单号、商品、支付事实、退款历史、必要联系信息和申请证据，不默认提供客户全部订单、全部聊天历史或账户信息。客服提交审核时必须保留 AI 原始建议、政策版本、引用事实、客服最终意见、修改内容、操作者和时间。

## 6. 首版非目标清单

- 部分退款、超额退款、优惠券/积分补偿；
- 换货、维修、退货物流、逆向取件和商品质检；
- 新订单、收款、分账、财务总账和营销自动化；
- 支付宝生产接入、真实支付凭证、支付网关实现和对账作业实现；
- 让 Agent、LLM、MCP 或浏览器直接调用退款/支付写操作；
- 完整客服团队、主管转派、会签、紧急越权和多商家租户；首版只实现当前客服自释放和 24 小时系统超时回收，离职和主管转派人工线下处理；
- 自动回填 `UNMATCHED` 历史订单归属；
- 管理员默认查看订单 PII；
- 前端大屏、复杂报表和与退款闭环无关的商品运营写操作；
- 在本 S0-01 中冻结 OpenAPI、错误码、SLO 或数据分级的具体文档内容；这些属于 S0-03。

## 7. 冻结决策索引

D01–D12 已由负责人接受，完整内容和唯一解释见 [`S0_DECISION_RECORD.md`](S0_DECISION_RECORD.md)。本 PRD 只保留产品路径和边界；后续实现必须同时满足该记录、S0-02 状态机和 S0-03 API 契约。
