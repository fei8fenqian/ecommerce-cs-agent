# S0：首版业务决策记录

状态：负责人已接受，首版冻结

适用范围：AI 售后与退款运营工作台第一条闭环——仅覆盖已支付订单的全额退款。

本记录是 D01–D12 的唯一冻结来源。`S0-01_PRD_REFUND_WORKBENCH.md`、`S0-02_ROLE_MATRIX_STATE_MACHINES.md`、`S0-03_API_SLO_DATA_CLASSIFICATION.md` 与 `PLAN_V6.md` 的相关表述必须与本记录一致；实现、测试和后续接口契约不得自行放宽或替换这些规则。

## D01：首版订单资格与申请时限

- 仅支持 CNY。
- 仅支持已支付、已确认归属、未发货订单。
- 支付成功后的 7 个自然日内可以申请。
- 已发货或已签收订单不进入首版退款闭环，转人工售后。
- 未确认归属的订单仍保持 `UNMATCHED`，不向客户暴露，不自动绑定。

影响：订单资格校验、客户提示、策略输入、人工售后分流和测试样例。

## D02：自动资格与财务分流

自动资格只适用于同时满足以下条件的全额退款：事实完整、未发货、无重复有效申请、无历史异常，且金额不超过 2000 CNY。客户确认仍是必需步骤。

不满足自动资格条件的退款申请一律转入财务审批；客服可以在人工路径中认领、补证和形成审核意见，但不能替代财务审批。AI 不能决定资格、金额或审批结果。

影响：策略引擎输入、`PENDING_CUSTOMER_CONFIRMATION` 与 `PENDING_FINANCE_APPROVAL` 的分流、客服工作台和验收测试。

## D03：订单事实与售后视图分离

- 支付状态、履约状态、售后状态分开保存。
- `AFTER_SALE_OPEN` 是由有效售后申请推导出的业务视图，不是覆盖支付或履约事实的订单状态。
- 售后申请取消、拒绝或过期，不得把支付或履约事实错误改写为其他状态。
- 退款成功后，退款事实由 `Refund` 与支付流水收敛；订单查询可以展示相应的派生结果，但不得混淆三类状态。

影响：`Order`/`AfterSaleRequest`/`Refund` 数据模型、状态机、查询接口和审计事件。

## D04：取消、补证与重新申请

- 客户仅可在 `SUBMITTED`、`EVIDENCE_PENDING`、`UNDER_REVIEW`、`PENDING_CUSTOMER_CONFIRMATION` 状态取消。
- 补证最多 2 次，每次补证期限为 72 小时。
- `CANCELLED` 可在原 7 个自然日申请期限内重新申请一次；重新申请应形成新的申请生命周期，不回退原申请状态。
- `REJECTED` 或 `EXPIRED` 后只能进入人工核验/客服入口，不能直接重新进入自动退款路径。

影响：客户命令、状态机、通知、补证待办和人工核验流程。

## D05：证据规则与留存

- 首版证据只允许 JPEG、PNG、WebP、PDF。
- 单文件最大 10 MB；每次申请最多 5 个文件。
- 申请关闭后证据保留 180 天。
- 原始证据不得进入日志或 AI Prompt；AI 只能接收按 scope 和字段白名单过滤后的必要事实或引用。

影响：上传契约、对象存储、数据分级、日志脱敏、AI 输入和清理策略。

## D06：金额、货币与可退余额

- 金额使用“分”的整数存储，仅支持 CNY。
- 金额唯一可信来源是支付流水。
- 可退余额 = 原支付金额 - 已成功退款金额 - 已创建/处理中的退款保留金额。
- 客户、客服、Agent 和 LLM 都不能自由输入或覆盖退款金额；首版只产生全额退款申请。

影响：金额模型、策略输入、支付请求、防超额退款校验、回调匹配和对账。

## D07：审批职责分离

- 同一人不得既提交客服审核意见又审批同一申请。
- 首版不做双人审批。
- 超过自动资格范围的申请由单个 `finance` 用户审批。

影响：角色矩阵、审批命令、审计记录和并发版本校验。

## D08：客服认领回收与转派

- 客服认领超过 24 小时，且申请尚未进入 `PENDING_FINANCE_APPROVAL` 或 `REFUND_PROCESSING` 时，系统自动回收认领。
- 离职回收和主管转派首版采用人工线下处理，不实现复杂团队或转派系统。

影响：claim/release 命令、队列防卡死、定时回收和审计。

## D09：退款政策职责分离

- 具体退款政策由 `operator` 管理。
- 政策创建/测试与审核/发布必须由两个不同的 `operator` 身份完成。
- 无法满足职责分离时，保持当前 `ACTIVE` 版本，不发布新政策。
- `admin` 只管理策略系统配置与授权，不创建、审核、发布或回滚具体退款政策。

影响：政策生命周期、运营权限、发布门禁、回滚和审计。

## D10：退款异常最终责任

- 支付失败、回调异常和对账差异由 `finance` 负责最终关闭。
- 系统负责重试、死信、通知和审计。
- `operator` 只处理政策问题，不关闭资金异常。

影响：异常状态、重试/死信、通知、对账待办和责任报表。

## D11：通知与待办 SLA

- 首版只提供工作台内通知和待办，不接短信或邮件。
- 补证 SLA 为 72 小时。
- 客服/财务待办目标为 24 小时。
- 超时进入异常队列。

影响：通知任务、工作台待办、超时监控和人工接管。

## D12：指标与 AI 人工选择

- 自动资格率 = 合格评估数 / 已完成资格评估数。
- 处理时长 = `SUBMITTED` 至最终状态。
- AI 建议的采用、修改或拒绝必须由客服显式选择，不能根据文本猜测。
- 效率指标在首批 100 个已关闭申请或运行 30 天后再评估。

影响：事件埋点、运营报表、AI 审计和效率指标验收。

## 冻结后的共同约束

1. 首版仍只处理全额退款；AI 只能整理事实、生成建议或触发受控评估，不能直接改变资格、金额、状态或资金。
2. 客户确认、财务审批、Outbox、支付回调验签、幂等、审计和资源级授权仍是独立的确定性边界。
3. 本记录只冻结业务规则，不代表业务代码、数据库迁移、支付集成或外部通知已经实现。
